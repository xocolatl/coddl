{
    "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
    "name": "Coddl Store",
    "scopeName": "source.coddl.cdstore",
    "fileTypes": ["cdstore"],
    "//": "A .cdstore is DML into coddl::storage — a bare sequence of .cd statements (insert, :=, union, Relation { … }). It is a syntactic subset of .cd, so it reuses the .cd grammar wholesale rather than maintaining a parallel keyword list that would drift.",
    "patterns": [
        { "include": "source.coddl" }
    ]
}
